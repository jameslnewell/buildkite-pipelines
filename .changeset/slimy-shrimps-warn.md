---
'@jameslnewell/buildkite-pipelines': minor
---

Added `.get*` methods to make inspecting `CommandStep`s easier during testing. 
Added overrides to `find*` which make inspecting found steps easier
